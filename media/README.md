# Media assets

- `icon.png` — 128×128 extension icon (referenced by `package.json`).
- `screenshot-main.png` — placeholder marketing screenshot referenced by the
  root `README.md`. Replace it with a real capture of the extension in action.

These images are placeholders. Regenerate `screenshot-main.png` by running the
extension (<kbd>F5</kbd>) and using **PocketView: Capture Screenshot**.
